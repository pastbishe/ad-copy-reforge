# Ad Copy Reforge

Веб-приложение для создания и управления рекламными копиями с использованием React, TypeScript, Vite и Supabase.

## 🚀 Быстрый старт

### Требования

- **Node.js** версии 18 или выше ([скачать](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js)
- **Git** (для клонирования репозитория)

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <YOUR_GIT_URL>
   cd ad-copy-reforge-main
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл `.env.local`:**
   
   Скопируйте шаблон:
   ```bash
   # Windows
   copy ENV-TEMPLATE.txt .env.local
   
   # Linux/Mac
   cp ENV-TEMPLATE.txt .env.local
   ```
   
   Отредактируйте `.env.local` и укажите ваш webhook URL:
   ```env
   VITE_N8N_SCRAPING_WEBHOOK_URL=https://n8n.praitech.io/webhook/scrape-competitor-simple
   ```

4. **Запустите проект:**
   
   **Windows:**
   ```bash
   # Вариант 1: Используйте готовый скрипт
   .\start-dev.bat
   
   # Вариант 2: Запустите вручную
   npm run dev
   ```
   
   **Linux/Mac:**
   ```bash
   # Вариант 1: Используйте готовый скрипт
   ./start-dev.sh
   
   # Вариант 2: Запустите вручную
   npm run dev
   ```

5. **Откройте в браузере:**
   
   Приложение будет доступно по адресу: **http://localhost:8080**

## 📋 Доступные команды

```bash
# Запуск dev-сервера
npm run dev

# Запуск с туннелем (для внешнего доступа)
npm run dev:tunnel

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Проверка кода линтером
npm run lint

# Проверка стабильности
npm run check:stability
```

## 🛠️ Технологии

- **Vite** - сборщик и dev-сервер
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **shadcn/ui** - компоненты UI
- **Supabase** - backend и база данных
- **React Router** - маршрутизация
- **Framer Motion** - анимации

## 📁 Структура проекта

```
ad-copy-reforge-main/
├── src/
│   ├── components/      # React компоненты
│   │   ├── ui/         # UI компоненты (shadcn)
│   │   └── ...         # Другие компоненты
│   ├── pages/          # Страницы приложения
│   ├── contexts/        # React контексты (Auth, Theme, Language)
│   ├── hooks/          # Пользовательские хуки
│   ├── lib/            # Утилиты
│   ├── integrations/   # Интеграции (Supabase)
│   └── config/         # Конфигурация
├── public/             # Статические файлы
├── supabase/           # Миграции базы данных
│   └── migrations/
├── scripts/            # Вспомогательные скрипты
└── .env.local          # Переменные окружения (не в git)
```

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env.local` в корне проекта (см. `ENV-TEMPLATE.txt`):

```env
VITE_N8N_SCRAPING_WEBHOOK_URL=your_webhook_url_here
```

### Supabase

Проект использует Supabase для backend. Настройки подключения находятся в:
- `src/integrations/supabase/client.ts`

Для настройки собственного проекта Supabase:
1. Создайте проект на [supabase.com](https://supabase.com)
2. Примените миграции из папки `supabase/migrations/`
3. Обновите URL и ключи в `client.ts`

## 🐛 Решение проблем

### Проблема: "npm не является внутренней или внешней командой"

**Решение:**
- Убедитесь, что Node.js установлен: `node --version`
- Перезапустите терминал после установки Node.js
- Проверьте, что Node.js добавлен в PATH

### Проблема: Ошибки при установке зависимостей

**Решение:**
```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json  # Linux/Mac
# или
Remove-Item -Recurse -Force node_modules, package-lock.json  # Windows

# Установите заново
npm install
```

### Проблема: Порт 8080 уже занят

**Решение:**
1. Найдите процесс, использующий порт:
   ```bash
   # Windows
   netstat -ano | findstr :8080
   
   # Linux/Mac
   lsof -i :8080
   ```
2. Остановите процесс или измените порт в `vite.config.ts`

### Проблема: Белый экран в браузере

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Убедитесь, что файл `.env.local` создан
4. Перезапустите dev-сервер

### Проблема: Ошибки с Supabase

**Решение:**
1. Проверьте URL и ключ в `src/integrations/supabase/client.ts`
2. Убедитесь, что проект Supabase активен
3. Проверьте RLS политики в Supabase Dashboard
4. Примените миграции из `supabase/migrations/`

## 📚 Дополнительная документация

- [QUICK-START.md](./QUICK-START.md) - Быстрый старт
- [WINDOWS-SETUP.md](./WINDOWS-SETUP.md) - Подробная настройка для Windows
- [DEV-SETUP.md](./DEV-SETUP.md) - Настройка dev-сервера
- [DEV-GUIDE.md](./DEV-GUIDE.md) - Руководство разработчика
- [DEPLOY.md](./DEPLOY.md) - Инструкции по деплою
- [N8N_SETUP.md](./N8N_SETUP.md) - Настройка N8N webhooks

## 🔗 Полезные ссылки

- [Vite Documentation](https://vitejs.dev/)
- [React Documentation](https://react.dev/)
- [Supabase Documentation](https://supabase.com/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [shadcn/ui Documentation](https://ui.shadcn.com/)

## 📝 Лицензия

Этот проект создан для личного использования.

---

**Удачной разработки! 🎉**
