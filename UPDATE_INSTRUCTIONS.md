# 📤 Инструкция по обновлению репозитория на GitHub

## ⚠️ ВАЖНО: Проблема безопасности

На GitHub уже есть файл `.env` с чувствительными данными. Его нужно удалить из репозитория!

## Шаг 1: Установка Git (если не установлен)

1. Скачайте Git: https://git-scm.com/download/win
2. Установите Git
3. Перезапустите терминал/PowerShell

Проверьте установку:
```bash
git --version
```

## Шаг 2: Быстрое обновление (автоматически)

**Windows:**
```bash
cd C:\Cursor\ad-copy-reforge-main\ad-copy-reforge-main
.\update-github.bat
```

**Linux/Mac:**
```bash
cd /path/to/ad-copy-reforge-main
chmod +x update-github.sh
./update-github.sh
```

## Шаг 3: Ручное обновление (если автоматическое не работает)

```bash
# 1. Перейдите в папку проекта
cd C:\Cursor\ad-copy-reforge-main\ad-copy-reforge-main

# 2. Инициализируйте git (если еще не сделано)
git init

# 3. Настройте remote
git remote add origin https://github.com/pastbishe/ad-copy-reforge.git
# или если remote уже есть:
git remote set-url origin https://github.com/pastbishe/ad-copy-reforge.git

# 4. Добавьте все файлы
git add .

# 5. Создайте коммит
git commit -m "Update project: improved README, gitignore, and setup instructions"

# 6. Переименуйте ветку в main (если нужно)
git branch -M main

# 7. Загрузите на GitHub
git push -u origin main
```

## 🔒 Удаление .env из репозитория (ВАЖНО!)

Файл `.env` не должен быть в GitHub! Выполните:

```bash
# Удалите .env из git (но оставьте локально)
git rm --cached .env

# Если файл уже в репозитории, удалите его из истории
git commit -m "Remove .env file from repository"

# Загрузите изменения
git push origin main --force
```

**ВНИМАНИЕ:** `--force` перезапишет историю. Используйте осторожно!

## 🔐 Авторизация на GitHub

При первом push GitHub попросит авторизацию:

1. **Используйте Personal Access Token** (НЕ пароль!)
2. Создайте токен:
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите scope: `repo` (полный доступ)
   - Скопируйте токен (он показывается только один раз!)

3. При запросе пароля введите токен

## ✅ Проверка

После успешной загрузки:
1. Откройте https://github.com/pastbishe/ad-copy-reforge
2. Убедитесь, что все файлы обновлены
3. Проверьте, что `.env` НЕ виден в репозитории
4. Проверьте, что новый README.md отображается

## 📝 Что было обновлено

- ✅ Обновлен `.gitignore` - добавлены все `.env` файлы
- ✅ Обновлен `README.md` - полные инструкции по установке
- ✅ Создан `GITHUB_SETUP.md` - инструкция по настройке GitHub
- ✅ Создан `SETUP_FROM_GITHUB.md` - инструкция для нового устройства
- ✅ Создан `UPDATE_INSTRUCTIONS.md` - эта инструкция
- ✅ Созданы скрипты `update-github.bat` и `update-github.sh`

## 🐛 Решение проблем

### Проблема: "git: command not found"
**Решение:** Установите Git (см. Шаг 1)

### Проблема: "Permission denied" или "Authentication failed"
**Решение:** 
- Используйте Personal Access Token вместо пароля
- Убедитесь, что токен имеет права `repo`

### Проблема: "remote origin already exists"
**Решение:**
```bash
git remote remove origin
git remote add origin https://github.com/pastbishe/ad-copy-reforge.git
```

### Проблема: Конфликт при push
**Решение:**
```bash
# Получите последние изменения
git pull origin main --rebase

# Разрешите конфликты, затем:
git push origin main
```

---

**После обновления проект будет готов к использованию на новом устройстве! 🎉**

