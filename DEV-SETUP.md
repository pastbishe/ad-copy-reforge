# 🚀 Настройка локального сервера разработки

## Быстрый запуск

### Вариант 1: Автоматический скрипт
```bash
./start-dev.sh
```

### Вариант 2: Ручной запуск
```bash
# Установка зависимостей (если нужно)
npm install

# Запуск сервера разработки
npm run dev
```

## 🌐 Доступ к сайту

После запуска сервера сайт будет доступен по адресу:
**http://localhost:8080**

## 🔄 Hot Module Replacement (HMR)

Сервер настроен для автоматического обновления страницы при изменении файлов:
- ✅ Изменения в React компонентах отображаются мгновенно
- ✅ Изменения в CSS/стилях применяются без перезагрузки
- ✅ Состояние приложения сохраняется при обновлении
- ✅ Интервал проверки изменений: 5 секунд

## 🛠️ Настройки сервера

Сервер настроен в файле `vite.config.ts`:
- **Порт**: 8080
- **Хост**: все интерфейсы (::)
- **HMR**: включен с overlay
- **Polling**: включен для надежного отслеживания изменений

## 📁 Структура проекта

```
src/
├── pages/           # Страницы приложения
├── components/      # React компоненты
├── contexts/        # React контексты
├── hooks/           # Пользовательские хуки
├── lib/             # Утилиты
└── integrations/    # Интеграции (Supabase)
```

## 🐛 Решение проблем

### Сервер не запускается
1. Проверьте, что порт 8080 свободен: `lsof -i :8080`
2. Остановите старые процессы: `pkill -f vite`
3. Переустановите зависимости: `rm -rf node_modules && npm install`

### Изменения не отображаются
1. Убедитесь, что HMR включен в браузере
2. Проверьте консоль браузера на ошибки
3. Попробуйте жесткое обновление: Ctrl+Shift+R

### Ошибки TypeScript
1. Проверьте синтаксис: `npm run lint`
2. Убедитесь, что все импорты корректны
3. Проверьте типы в `src/integrations/supabase/types.ts`

## 🎯 Полезные команды

```bash
# Запуск с туннелем (для внешнего доступа)
npm run dev:tunnel

# Сборка для продакшена
npm run build

# Предварительный просмотр сборки
npm run preview

# Проверка кода
npm run lint
```

## 📱 Мобильная разработка

Для тестирования на мобильных устройствах:
1. Узнайте IP адрес: `ifconfig | grep inet`
2. Откройте `http://YOUR_IP:8080` на мобильном устройстве
3. Убедитесь, что устройства в одной сети

## 🔧 Дополнительные настройки

### Изменение порта
Отредактируйте `vite.config.ts`:
```typescript
server: {
  port: 3000, // новый порт
}
```

### Отключение HMR
В `vite.config.ts`:
```typescript
server: {
  hmr: false
}
```

---

**Удачной разработки! 🎉**
