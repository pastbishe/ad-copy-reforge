# 🚀 Автоматический деплой - АКТИВЕН

## ✅ Что сейчас работает:

Фоновый процесс следит за всеми изменениями в папке `src/` и автоматически деплоит их на Vercel.

**Ваш Production URL:**
### 🌐 https://ad-copy-reforge-2t2ja6rgp-pastbishes-projects.vercel.app

---

## 📋 Как это работает:

1. Вы (или AI) сохраняете изменения в любом файле в `src/`
2. Через 5 секунд скрипт обнаруживает изменения
3. Автоматически делается:
   - `git add .`
   - `git commit -m "Auto-deploy #N"`
   - `git push origin main`
4. Vercel автоматически начинает деплой
5. Через **~1-2 минуты** изменения появляются на сайте

---

## ⚡️ Статус автодеплоя:

**✅ АКТИВЕН** (Process ID: 40863)

Процесс работает в фоне и будет продолжать работать, пока вы не остановите его или не перезагрузите компьютер.

---

## 🎮 Управление автодеплоем:

### Остановить автодеплой:
```bash
pkill -f auto-deploy
```

### Запустить заново:
```bash
cd /Users/macbookair/Desktop/adcopy/ad-copy-reforge
./auto-deploy.sh
```

Или короче:
```bash
npm run dev:auto-deploy
```

### Посмотреть статус:
```bash
ps aux | grep auto-deploy | grep -v grep
```

---

## 📊 Мониторинг деплоев:

- **Vercel Dashboard:** https://vercel.com/pastbishes-projects/ad-copy-reforge
- **Git History:** `git log --oneline -n 10` (покажет последние 10 автодеплоев)
- **Текущий статус:** Проверьте Vercel dashboard для статуса деплоя

---

## 💡 Советы:

- Изменения появляются на сайте через 1-2 минуты после сохранения файла
- Если сделали много мелких правок, подождите 1-2 минуты перед следующей проверкой
- Каждый деплой создает отдельную превью-версию на Vercel
- Скрипт следит только за папкой `src/` (не реагирует на изменения в других папках)

---

## 🎉 Готово к работе!

Теперь просто сохраняйте изменения в коде, и они автоматически появятся на вашем сайте через пару минут.

