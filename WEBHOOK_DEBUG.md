# Отладка вебхука N8N

## Проблема
Вебхук не триггерится, сайт застрял на экране загрузки.

## Шаги для решения

### 1. Проверьте переменную окружения

Убедитесь, что файл `.env.local` существует в корне проекта `ad-copy-reforge/` и содержит:

```env
VITE_N8N_SCRAPING_WEBHOOK_URL=https://ваш-n8n-инстанс.com/webhook/scrape-competitor-simple
```

**Пример:**
```env
VITE_N8N_SCRAPING_WEBHOOK_URL=https://n8n.praitech.io/webhook/scrape-competitor-simple
```

### 2. Перезапустите dev сервер

⚠️ **ВАЖНО:** После создания или изменения `.env.local` нужно перезапустить dev сервер!

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

Vite загружает переменные окружения только при запуске, поэтому изменения в `.env.local` требуют перезапуска.

### 3. Проверьте консоль браузера

1. Откройте DevTools (F12 или Cmd+Option+I)
2. Перейдите на вкладку **Console**
3. Попробуйте импортировать ссылку рекламы конкурента
4. Проверьте логи:

   - ✅ `N8N Webhook URL настроен: ...` - вебхук настроен
   - ⚠️ `N8N Webhook URL не настроен` - переменная окружения не загружена
   - 🚀 `Запуск скрапинга: ...` - начало процесса
   - 📤 `Отправка запроса в N8N webhook: ...` - запрос отправляется
   - 📥 `Ответ от N8N webhook: ...` - получен ответ
   - ✅ `N8N webhook успешно вызван` - вебхук вызван успешно
   - ❌ Ошибки - проверьте сообщение об ошибке

### 4. Проверьте Network вкладку

1. Откройте DevTools → **Network**
2. Попробуйте импортировать ссылку
3. Найдите запрос к вашему N8N webhook URL
4. Проверьте:
   - **Status**: должен быть 200 (OK)
   - **Request Payload**: должен содержать `sourceUrl` и `userId`
   - **Response**: ответ от вебхука

### 5. Проверьте URL вебхука

Убедитесь, что URL вебхука правильный и доступен:

```bash
# Проверьте доступность вебхука
curl -X POST https://ваш-n8n-инстанс.com/webhook/scrape-competitor-simple \
  -H "Content-Type: application/json" \
  -d '{"sourceUrl":"https://example.com","userId":"test-user-id"}'
```

### 6. Проверьте N8N workflow

1. Откройте ваш N8N инстанс
2. Проверьте, что workflow активен
3. Перейдите в **Executions** (Выполнения)
4. Проверьте, есть ли новые выполнения после попытки импорта
5. Если есть ошибки, проверьте логи выполнения

### 7. Если вебхук не вызывается

**Проблема:** Переменная окружения не загружена

**Решение:**
1. Убедитесь, что файл `.env.local` находится в папке `ad-copy-reforge/` (не в `adcopy/`)
2. Убедитесь, что имя переменной начинается с `VITE_`
3. Перезапустите dev сервер
4. Проверьте консоль браузера на наличие предупреждения

### 8. Если вебхук вызывается, но фотографии не загружаются

Код теперь автоматически проверяет статус задания каждые 5 секунд. Если фотографии не загружаются:

1. Проверьте, что таблица `scrape_jobs` создана (см. `APPLY_MIGRATION.md`)
2. Проверьте, что таблица `competitor_photos` создана
3. Проверьте логи в консоли браузера
4. Проверьте статус задания в Supabase Dashboard

## Что изменилось в коде

1. ✅ Добавлено логирование для отладки
2. ✅ Добавлена периодическая проверка статуса задания (каждые 5 секунд)
3. ✅ Улучшена обработка ошибок
4. ✅ Код не застревает на загрузке, если фотографий еще нет

## Тестирование

После настройки вебхука:

1. Откройте консоль браузера (F12)
2. Введите URL рекламы конкурента
3. Нажмите Import
4. Следите за логами в консоли
5. Проверьте, что вебхук вызывается

## Дополнительная информация

- **Файл миграции:** `supabase/migrations/20251030120000_competitor_content.sql`
- **Функция вебхука:** `src/lib/scrapingUtils.ts` → `startScrapingJobSimple`
- **Компонент импорта:** `src/pages/Studio.tsx` → `handleImport`





