# 🚀 Быстрая установка проекта с GitHub

Эта инструкция для установки проекта на **новом устройстве** после клонирования с GitHub.

## Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/ad-copy-reforge.git
cd ad-copy-reforge
```

## Шаг 2: Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты из `package.json`.

## Шаг 3: Создание файла .env.local

Создайте файл `.env.local` в корне проекта:

**Windows:**
```bash
copy ENV-TEMPLATE.txt .env.local
```

**Linux/Mac:**
```bash
cp ENV-TEMPLATE.txt .env.local
```

Откройте `.env.local` и укажите ваши настройки:
```env
VITE_N8N_SCRAPING_WEBHOOK_URL=https://n8n.praitech.io/webhook/scrape-competitor-simple
```

## Шаг 4: Запуск проекта

**Windows:**
```bash
.\start-dev.bat
# или
npm run dev
```

**Linux/Mac:**
```bash
./start-dev.sh
# или
npm run dev
```

## Шаг 5: Открытие в браузере

Откройте браузер и перейдите на: **http://localhost:8080**

## ✅ Готово!

Проект должен запуститься и работать. Если возникнут проблемы, см. раздел "Решение проблем" в [README.md](./README.md).

## 📝 Важные замечания

1. **Файл `.env.local` не хранится в GitHub** - его нужно создавать на каждом устройстве
2. **Supabase настройки** уже настроены в коде, но при необходимости их можно изменить в `src/integrations/supabase/client.ts`
3. **Миграции базы данных** находятся в `supabase/migrations/` - примените их в вашем Supabase проекте

---

**Удачной работы! 🎉**

