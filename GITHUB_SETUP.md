# 📤 Инструкция по загрузке проекта на GitHub

Эта инструкция поможет вам загрузить проект на GitHub, чтобы можно было клонировать его на новом устройстве.

## Шаг 1: Установка Git (если не установлен)

1. Скачайте Git с официального сайта: https://git-scm.com/download/win
2. Установите Git, следуя инструкциям установщика
3. Перезапустите терминал/PowerShell

Проверьте установку:
```bash
git --version
```

## Шаг 2: Инициализация репозитория

Откройте терминал в папке проекта и выполните:

```bash
cd C:\Cursor\ad-copy-reforge-main\ad-copy-reforge-main

# Инициализируйте git репозиторий
git init

# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Создайте первый коммит
git commit -m "Initial commit: Ad Copy Reforge project"
```

## Шаг 3: Создание репозитория на GitHub

1. Откройте [GitHub.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `ad-copy-reforge` (или любое другое имя)
   - **Description**: "Ad Copy Reforge - веб-приложение для создания рекламных копий"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

## Шаг 4: Подключение к GitHub и загрузка

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/ad-copy-reforge.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте **Personal Access Token** вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите scope: `repo` (полный доступ к репозиториям)

## Шаг 5: Проверка

Откройте ваш репозиторий на GitHub и убедитесь, что все файлы загружены.

## 🔄 Обновление проекта на GitHub

Когда вы вносите изменения в проект, обновите GitHub:

```bash
# Проверьте статус изменений
git status

# Добавьте измененные файлы
git add .

# Создайте коммит с описанием изменений
git commit -m "Описание ваших изменений"

# Загрузите изменения на GitHub
git push
```

## 📥 Клонирование на новом устройстве

На новом устройстве выполните:

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/ad-copy-reforge.git

# Перейдите в папку проекта
cd ad-copy-reforge

# Установите зависимости
npm install

# Создайте файл .env.local (см. ENV-TEMPLATE.txt)
copy ENV-TEMPLATE.txt .env.local  # Windows
# или
cp ENV-TEMPLATE.txt .env.local    # Linux/Mac

# Отредактируйте .env.local и укажите ваши настройки

# Запустите проект
npm run dev
```

## ⚠️ Важные замечания

1. **Файл `.env.local` НЕ попадет в GitHub** (он в `.gitignore`)
2. **Папка `node_modules` НЕ попадет в GitHub** (она в `.gitignore`)
3. **Все чувствительные данные** (ключи, пароли) должны быть в `.env.local`, а не в коде
4. **Проверьте `.gitignore`** перед первым коммитом

## 🐛 Решение проблем

### Проблема: "git: command not found"

**Решение:** Установите Git (см. Шаг 1)

### Проблема: "Permission denied" при push

**Решение:** 
- Проверьте правильность URL репозитория
- Используйте Personal Access Token вместо пароля
- Убедитесь, что у вас есть права на запись в репозиторий

### Проблема: "fatal: remote origin already exists"

**Решение:**
```bash
# Удалите существующий remote
git remote remove origin

# Добавьте заново
git remote add origin https://github.com/YOUR_USERNAME/ad-copy-reforge.git
```

### Проблема: Конфликт при push

**Решение:**
```bash
# Получите последние изменения
git pull origin main --rebase

# Разрешите конфликты (если есть)
# Затем снова попробуйте
git push
```

---

**Готово! Теперь ваш проект на GitHub и готов к использованию на любом устройстве! 🎉**









