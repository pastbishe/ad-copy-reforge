# 🚀 Быстрый старт на Windows

## ✅ Что нужно сделать:

### 1. Установить Node.js (если еще не установлен)
- Скачайте с https://nodejs.org/ (LTS версию)
- Установите и перезапустите терминал

### 2. Установить зависимости
```powershell
cd C:\Cursor\ad-copy-reforge-main\ad-copy-reforge-main
npm install
```

### 3. Создать файл .env.local
Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
VITE_N8N_SCRAPING_WEBHOOK_URL=https://n8n.praitech.io/webhook/scrape-competitor-simple
```

**Как создать:**
- Скопируйте файл `ENV-TEMPLATE.txt` и переименуйте в `.env.local`
- Или создайте новый файл `.env.local` вручную

### 4. Запустить проект

**Вариант 1:** Дважды кликните на `start-dev.bat`

**Вариант 2:** В терминале:
```powershell
npm run dev
```

### 5. Открыть в браузере
Перейдите на: **http://localhost:8080**

## ✅ Готово!

Если что-то не работает - см. `WINDOWS-SETUP.md` для подробных инструкций.

