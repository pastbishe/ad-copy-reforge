# 🚀 Руководство по разработке с live preview

## Способы просмотра изменений в реальном времени

### ⚡️ Способ 1: Локальный сервер (РЕКОМЕНДУЕТСЯ)

**Мгновенные изменения без перезагрузки страницы!**

```bash
npm run dev
```

Откройте в браузере: **http://localhost:8080**

✅ **Преимущества:**
- Изменения отображаются мгновенно (Hot Module Replacement)
- Не нужно перезагружать страницу
- Самый быстрый способ для разработки

---

### 📱 Способ 2: Доступ из локальной сети

Для просмотра на телефоне или другом компьютере в той же сети:

```bash
npm run dev
```

Откройте в браузере: **http://192.168.68.54:8080**

✅ **Подходит для:**
- Тестирование на мобильных устройствах
- Показ изменений коллегам в той же сети

---

### 🌍 Способ 3: Публичный доступ через туннель

Для доступа из интернета (с любого устройства в мире):

```bash
npm run dev:tunnel
```

Вы получите публичный URL вида: `https://your-url.loca.lt`

✅ **Подходит для:**
- Показ клиенту или удаленной команде
- Тестирование на устройствах вне локальной сети
- Временный доступ без деплоя

⚠️ **Примечание:** При первом посещении может попросить подтвердить IP

---

### 🔄 Способ 4: Vercel Preview (для шаринга стабильных версий)

Каждый git commit автоматически создает preview URL на Vercel:

```bash
git add .
git commit -m "Ваше описание изменений"
git push
```

Vercel пришлет вам уникальный URL для этого коммита.

Production URL: **https://ad-copy-reforge-2t2ja6rgp-pastbishes-projects.vercel.app**

✅ **Подходит для:**
- Шаринг стабильных версий
- Долгосрочный доступ
- A/B тестирование разных версий

---

## 💡 Рекомендуемый workflow

1. **Во время разработки:** Используйте `npm run dev` (Способ 1)
2. **Для показа в процессе работы:** `npm run dev:tunnel` (Способ 3)
3. **Для демо готовой версии:** Сделайте `git push` на Vercel (Способ 4)

---

## 🛠 Дополнительные команды

```bash
# Обычная разработка
npm run dev

# С публичным туннелем
npm run dev:tunnel

# Сборка для продакшена
npm run build

# Предпросмотр production build
npm run preview

# Линтер
npm run lint
```

---

## 🔥 Hot Module Replacement (HMR)

Ваш проект уже настроен с HMR! Это значит:

- ✨ Изменения в CSS/SCSS применяются без перезагрузки
- ⚛️ React компоненты обновляются с сохранением состояния
- 🚀 Изменения видны за миллисекунды

**Просто сохраните файл - браузер обновится автоматически!**


