# 🔍 Диагностика проблем с сайтом

## ✅ Проверка 1: Сервер запущен

Сервер работает на порту 8080. Проверьте доступность:

```bash
# Проверка порта
lsof -i :8080

# Проверка доступности
curl http://localhost:8080
```

**Ожидаемый результат:** HTML страница должна загрузиться

---

## ✅ Проверка 2: Откройте сайт в браузере

1. Откройте браузер
2. Перейдите по адресу: **http://localhost:8080**
3. Если страница не загружается, проверьте:
   - Не блокирует ли файрвол порт 8080
   - Не занят ли порт другим процессом

---

## ✅ Проверка 3: Консоль браузера

1. Откройте DevTools (F12 или Cmd+Option+I)
2. Перейдите на вкладку **Console**
3. Проверьте наличие ошибок (красные сообщения)

**Частые ошибки:**

### Ошибка: "Failed to fetch" или "Network error"
- **Причина:** Проблема с подключением к Supabase
- **Решение:** Проверьте интернет-соединение и доступность Supabase

### Ошибка: "Cannot read property of undefined"
- **Причина:** Проблема с инициализацией компонентов
- **Решение:** Перезапустите dev сервер: `npm run dev`

### Ошибка: "Module not found"
- **Причина:** Отсутствуют зависимости
- **Решение:** Выполните `npm install`

---

## ✅ Проверка 4: База данных Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Выполните проверочный запрос:

```sql
SELECT COUNT(*) FROM auth.users;
```

**Если ошибка:** Проверьте подключение к Supabase

---

## ✅ Проверка 5: Переменные окружения

Проверьте наличие файла `.env.local` в корне проекта:

```bash
ls -la .env.local
```

Если файла нет, создайте его (если требуется для N8N webhook):

```env
VITE_N8N_SCRAPING_WEBHOOK_URL=https://ваш-n8n-инстанс.com/webhook/scrape-competitor-simple
```

**Важно:** После создания `.env.local` перезапустите dev сервер!

---

## ✅ Проверка 6: Перезапуск сервера

Если ничего не помогает, перезапустите сервер:

```bash
# Остановите текущий сервер (Ctrl+C в терминале)
# Затем запустите снова:
cd /Users/macbookair/Desktop/adcopy/ad-copy-reforge
npm run dev
```

---

## 🐛 Частые проблемы и решения

### Проблема: Белый экран
**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Перезапустите dev сервер

### Проблема: Страница загружается, но ничего не отображается
**Решение:**
1. Проверьте консоль браузера на ошибки
2. Проверьте вкладку Network в DevTools
3. Убедитесь, что все файлы загружаются (статус 200)

### Проблема: Ошибки с Supabase
**Решение:**
1. Проверьте URL и ключ в `src/integrations/supabase/client.ts`
2. Убедитесь, что проект Supabase активен
3. Проверьте RLS политики в Supabase

### Проблема: Ошибки с таблицей photos
**Решение:**
1. Выполните SQL из `fix_photos_table.sql` в Supabase SQL Editor
2. Убедитесь, что колонка `photo_url` nullable

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. **Скопируйте ошибки из консоли браузера**
2. **Проверьте логи сервера** (в терминале, где запущен `npm run dev`)
3. **Проверьте Network tab** в DevTools на наличие failed запросов

---

## ✅ Быстрая проверка работоспособности

Выполните в терминале:

```bash
# 1. Проверка сервера
curl http://localhost:8080 | head -20

# 2. Проверка зависимостей
npm list react react-dom

# 3. Проверка порта
lsof -i :8080
```

Все команды должны выполниться без ошибок.



