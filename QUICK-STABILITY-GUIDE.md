# 🛡️ Быстрый гайд по стабильности сайта

## ✅ Что было сделано

Создана система защиты от падения сайта:

1. **Error Boundary** - перехватывает ошибки React
2. **Глобальная обработка ошибок** - перехватывает все JavaScript ошибки
3. **Автоматические проверки** - проверяет стабильность при изменениях
4. **Логирование ошибок** - все ошибки сохраняются в localStorage

## 🚀 Как использовать

### Проверка стабильности перед коммитом
```bash
npm run check:stability
```

### Автоматический мониторинг во время разработки
```bash
npm run watch:stability
```

### Запуск dev сервера с мониторингом
```bash
npm run dev:watch
```

## 🔍 Просмотр ошибок

Откройте консоль браузера (F12) и выполните:
```javascript
// Глобальные ошибки
console.table(JSON.parse(localStorage.getItem('appErrorLogs') || '[]'))

// Ошибки React
console.table(JSON.parse(localStorage.getItem('errorLogs') || '[]'))
```

## 📝 Что происходит при ошибке

1. Ошибка перехватывается ErrorBoundary или ErrorHandler
2. Пользователю показывается дружелюбное сообщение
3. Ошибка логируется в localStorage
4. Сайт продолжает работать (не падает полностью)

## ⚠️ Важно

- Проверка стабильности автоматически запускается перед сборкой (`npm run build`)
- Для быстрого запуска dev сервера используйте `npm run dev` (без проверки)
- Для разработки с мониторингом используйте `npm run dev:watch`

## 📚 Подробная документация

См. [STABILITY.md](./STABILITY.md) для полной документации.

