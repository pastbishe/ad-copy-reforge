# 🛡️ Система мониторинга стабильности сайта

Эта система автоматически отслеживает стабильность сайта и предотвращает его падение после обновлений кода.

> **См. также:** [NETWORK-ERRORS.md](./NETWORK-ERRORS.md) - улучшенная обработка сетевых ошибок

## 📋 Что было реализовано

### 1. Error Boundary компонент
- **Файл**: `src/components/ErrorBoundary.tsx`
- **Назначение**: Перехватывает все ошибки React и предотвращает полное падение приложения
- **Функции**:
  - Автоматический перехват ошибок React
  - Логирование ошибок в localStorage
  - Автоматический сброс после 5 секунд (для некритических ошибок)
  - Пользовательский интерфейс с кнопками восстановления

### 2. Глобальная обработка ошибок
- **Файл**: `src/lib/errorHandler.ts`
- **Назначение**: Обрабатывает все глобальные ошибки JavaScript
- **Функции**:
  - Перехват необработанных ошибок (`window.onerror`)
  - Перехват необработанных промисов (`unhandledrejection`)
  - Перехват ошибок загрузки ресурсов (изображения, скрипты, стили)
  - Логирование всех ошибок в localStorage
  - Предотвращение полного падения приложения

### 3. Интеграция в приложение
- **App.tsx**: Обернут в `<ErrorBoundary>`
- **main.tsx**: Инициализирован глобальный обработчик ошибок
- **QueryClient**: Настроены повторные попытки для запросов

### 4. Скрипты проверки стабильности

#### `scripts/check-stability.js`
Проверяет:
- ✅ Наличие критических файлов
- ✅ Корректность package.json
- ✅ Интеграцию ErrorBoundary
- ✅ Интеграцию ErrorHandler
- ✅ Ошибки TypeScript
- ✅ Ошибки ESLint
- ✅ Успешность сборки проекта

#### `scripts/watch-stability.js`
Автоматически запускает проверки при изменении файлов:
- Отслеживает изменения в `src/`
- Отслеживает изменения конфигурационных файлов
- Запускает проверку через 2 секунды после последнего изменения

## 🚀 Использование

### Ручная проверка стабильности
```bash
npm run check:stability
```

### Автоматический мониторинг
```bash
npm run watch:stability
```

### Запуск dev сервера с проверкой
```bash
npm run dev
# Автоматически запустит проверку перед стартом (predev hook)
```

### Запуск dev сервера с мониторингом
```bash
npm run dev:watch
# Запустит dev сервер и мониторинг одновременно
```

## 🔍 Просмотр логов ошибок

Ошибки сохраняются в localStorage браузера:
- `appErrorLogs` - логи глобальных ошибок
- `errorLogs` - логи ошибок React (ErrorBoundary)

Для просмотра в консоли браузера:
```javascript
// Глобальные ошибки
JSON.parse(localStorage.getItem('appErrorLogs') || '[]')

// Ошибки React
JSON.parse(localStorage.getItem('errorLogs') || '[]')
```

## 🛠️ Как это работает

1. **При возникновении ошибки**:
   - ErrorBoundary перехватывает ошибки React
   - ErrorHandler перехватывает глобальные ошибки JavaScript
   - Ошибка логируется в localStorage
   - Пользователю показывается дружелюбное сообщение вместо белого экрана

2. **При обновлении кода**:
   - Скрипт `watch-stability.js` отслеживает изменения
   - Автоматически запускается проверка стабильности
   - Если обнаружены проблемы - выводится предупреждение

3. **При сборке**:
   - Проверяется наличие всех критических файлов
   - Проверяется корректность TypeScript
   - Проверяется успешность сборки

## 📝 Рекомендации

1. **Всегда проверяйте стабильность перед коммитом**:
   ```bash
   npm run check:stability
   ```

2. **Используйте мониторинг во время разработки**:
   ```bash
   npm run watch:stability
   ```

3. **Проверяйте логи ошибок в браузере**:
   - Откройте DevTools (F12)
   - Перейдите в Application > Local Storage
   - Проверьте `appErrorLogs` и `errorLogs`

4. **При возникновении ошибок**:
   - Проверьте консоль браузера
   - Проверьте логи в localStorage
   - Используйте ErrorBoundary UI для восстановления

## 🔧 Настройка

### Отключение автоматической проверки перед dev
Удалите строку из `package.json`:
```json
"predev": "npm run check:stability"
```

### Изменение интервала проверки в watch-stability.js
В файле `scripts/watch-stability.js` измените значение:
```javascript
setTimeout(() => {
  runStabilityCheck();
}, 2000); // Измените 2000 на нужное значение (в миллисекундах)
```

### Изменение количества хранимых логов
В файле `src/lib/errorHandler.ts` измените:
```typescript
private maxLogs = 50; // Измените на нужное значение
```

## ✅ Результат

Теперь ваш сайт:
- ✅ Не падает полностью при ошибках
- ✅ Показывает дружелюбные сообщения об ошибках
- ✅ Автоматически логирует все ошибки
- ✅ Проверяется на стабильность при изменениях
- ✅ Имеет систему восстановления после ошибок
- ✅ Обрабатывает сетевые ошибки с понятными сообщениями
- ✅ Автоматически повторяет запросы при временных сбоях
- ✅ Преобразует технические ошибки в понятные сообщения для пользователя

## 🌐 Обработка сетевых ошибок

Система автоматически обрабатывает сетевые ошибки типа "Failed to fetch":
- Преобразует их в понятные сообщения для пользователя
- Автоматически повторяет запросы при временных сбоях
- Логирует все сетевые ошибки для диагностики

**Подробнее:** [NETWORK-ERRORS.md](./NETWORK-ERRORS.md)

