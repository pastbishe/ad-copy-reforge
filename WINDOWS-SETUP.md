# Инструкция по настройке проекта на Windows

## ✅ Шаг 1: Установка Node.js

Если Node.js не установлен:

1. Скачайте Node.js с официального сайта: https://nodejs.org/
2. Установите LTS версию (рекомендуется)
3. Перезапустите терминал/PowerShell после установки
4. Проверьте установку:
   ```powershell
   node --version
   npm --version
   ```

## ✅ Шаг 2: Установка зависимостей

Откройте PowerShell или Command Prompt в папке проекта и выполните:

```powershell
cd C:\Cursor\ad-copy-reforge-main\ad-copy-reforge-main
npm install
```

Это установит все необходимые зависимости проекта.

## ✅ Шаг 3: Создание файла .env.local

Создайте файл `.env.local` в корне проекта (`ad-copy-reforge-main/`) со следующим содержимым:

```env
# URL webhook для N8N скрапинга конкурентов
VITE_N8N_SCRAPING_WEBHOOK_URL=https://n8n.praitech.io/webhook/scrape-competitor-simple
```

**Важно:** 
- Замените URL на ваш реальный webhook URL, если он отличается
- Файл `.env.local` уже добавлен в `.gitignore`, поэтому не попадет в git

## ✅ Шаг 4: Запуск проекта

### Вариант 1: Использовать готовый скрипт (Windows)

Дважды кликните на файл `start-dev.bat` или выполните в терминале:

```powershell
.\start-dev.bat
```

### Вариант 2: Запустить вручную

```powershell
npm run dev
```

Сервер будет доступен по адресу: **http://localhost:8080**

## ✅ Шаг 5: Проверка работы

1. Откройте браузер и перейдите на http://localhost:8080
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Если есть ошибки - см. раздел "Устранение проблем"

## 🐛 Устранение проблем

### Проблема: "npm не является внутренней или внешней командой"

**Решение:** 
- Убедитесь, что Node.js установлен
- Перезапустите терминал после установки Node.js
- Проверьте, что Node.js добавлен в PATH

### Проблема: Ошибки при установке зависимостей

**Решение:**
```powershell
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и package-lock.json
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json

# Установите заново
npm install
```

### Проблема: Порт 8080 уже занят

**Решение:**
1. Найдите процесс, использующий порт 8080:
   ```powershell
   netstat -ano | findstr :8080
   ```
2. Остановите процесс или измените порт в `vite.config.ts`

### Проблема: Белый экран в браузере

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Убедитесь, что файл `.env.local` создан и перезапустите сервер
4. Проверьте, что Supabase URL и ключ правильные в `src/integrations/supabase/client.ts`

### Проблема: Ошибки с Supabase

**Решение:**
1. Проверьте URL и ключ в `src/integrations/supabase/client.ts`
2. Убедитесь, что проект Supabase активен
3. Проверьте RLS политики в Supabase Dashboard

## 📝 Дополнительная информация

- **Основные команды:**
  - `npm run dev` - запуск dev сервера
  - `npm run build` - сборка для production
  - `npm run lint` - проверка кода линтером
  - `npm run preview` - предпросмотр production сборки

- **Структура проекта:**
  - `src/` - исходный код приложения
  - `public/` - статические файлы
  - `supabase/migrations/` - миграции базы данных
  - `.env.local` - переменные окружения (не в git)

## 🔗 Полезные ссылки

- GitHub репозиторий: https://github.com/pastbishe/ad-copy-reforge.git
- Supabase Dashboard: https://supabase.com/dashboard
- Документация Vite: https://vitejs.dev/
- Документация React: https://react.dev/

