# 🌐 Улучшенная обработка сетевых ошибок

## ✅ Что было сделано

### 1. Утилиты для обработки сетевых ошибок (`src/lib/networkUtils.ts`)

Создан модуль с функциями для:
- **Определения сетевых ошибок** - проверяет, является ли ошибка сетевой и может ли быть повторена
- **Понятных сообщений** - преобразует технические ошибки в понятные сообщения для пользователя
- **Retry логики** - автоматически повторяет запросы при сетевых ошибках
- **Обработки таймаутов** - обрабатывает ошибки превышения времени ожидания

### 2. Улучшенный Supabase клиент (`src/integrations/supabase/client.ts`)

- Добавлен таймаут 30 секунд для всех запросов
- Автоматический retry при сетевых ошибках
- Улучшенные сообщения об ошибках
- Функция `safeSupabaseQuery` для безопасных запросов

### 3. Глобальная обработка сетевых ошибок (`src/lib/errorHandler.ts`)

- Автоматическое определение сетевых ошибок
- Логирование с понятными сообщениями
- Отслеживание retryable ошибок

### 4. Обновлена обработка ошибок в компонентах

- `Login.tsx` - улучшенные сообщения об ошибках входа
- `Signup.tsx` - улучшенные сообщения об ошибках регистрации
- `Studio.tsx` - улучшенные сообщения об ошибках скрапинга

## 📋 Типы обрабатываемых ошибок

### Сетевые ошибки
- `Failed to fetch` - проблема с подключением
- `NetworkError` - общая сетевая ошибка
- `Network request failed` - неудачный сетевой запрос
- `ERR_NETWORK` - ошибка сети
- `ERR_INTERNET_DISCONNECTED` - нет интернета
- `ERR_CONNECTION_REFUSED` - соединение отклонено
- `ERR_CONNECTION_TIMED_OUT` - таймаут соединения

### HTTP ошибки
- `408` - Request Timeout
- `429` - Too Many Requests
- `500` - Internal Server Error
- `502` - Bad Gateway
- `503` - Service Unavailable
- `504` - Gateway Timeout

## 💬 Примеры сообщений для пользователя

### Сетевые ошибки
```
"Проблема с подключением к интернету. Проверьте ваше интернет-соединение и попробуйте снова."
```

### Таймауты
```
"Превышено время ожидания ответа. Сервер не отвечает. Попробуйте позже."
```

### Ошибки авторизации
```
"Ошибка авторизации. Пожалуйста, войдите в систему снова."
```

### Ошибки сервера
```
"Ошибка на сервере. Мы уже работаем над её исправлением. Попробуйте позже."
```

## 🔧 Использование

### Базовое использование
```typescript
import { handleNetworkError } from '@/lib/networkUtils';

try {
  // Ваш код
} catch (error) {
  const { message, isRetryable } = handleNetworkError(error);
  // message - понятное сообщение для пользователя
  // isRetryable - можно ли повторить запрос
}
```

### С retry логикой
```typescript
import { withRetry } from '@/lib/networkUtils';

const result = await withRetry(async () => {
  return await fetch('/api/data');
}, {
  maxRetries: 3,
  retryDelay: 1000,
});
```

### Безопасные Supabase запросы
```typescript
import { safeSupabaseQuery } from '@/integrations/supabase/client';

const { data, error, userMessage } = await safeSupabaseQuery(async () => {
  return await supabase.from('photos').select('*');
});

if (error) {
  // userMessage содержит понятное сообщение для пользователя
  toast({ description: userMessage });
}
```

## 🎯 Результат

Теперь при сетевых ошибках:
- ✅ Пользователь видит понятные сообщения вместо технических ошибок
- ✅ Автоматически повторяются запросы при временных сбоях
- ✅ Все ошибки логируются с дополнительной информацией
- ✅ Ошибка "Failed to fetch" преобразуется в понятное сообщение

## 📝 Примеры обработки

### До улучшений
```
Ошибка: Failed to fetch
```

### После улучшений
```
Ошибка: Проблема с подключением к интернету. Проверьте ваше интернет-соединение и попробуйте снова.
```

## 🔍 Отладка

Для просмотра сетевых ошибок в консоли браузера:
```javascript
// Глобальные ошибки (включая сетевые)
const logs = JSON.parse(localStorage.getItem('appErrorLogs') || '[]');
const networkErrors = logs.filter(log => log.source === 'network');
console.table(networkErrors);
```

